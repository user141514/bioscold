# Deprecated Figure Set

This PaperBanana from-scratch figure set is not active for the current JCIM submission package.

Use the previous harmonized figure set instead:

`../frozen_jcim_style_v4/`

The active set is recorded in:

`../ACTIVE_FIGURE_SET.md`
