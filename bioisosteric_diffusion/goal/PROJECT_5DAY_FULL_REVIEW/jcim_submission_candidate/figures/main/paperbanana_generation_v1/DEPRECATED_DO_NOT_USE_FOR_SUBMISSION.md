# Deprecated

This PaperBanana/DeepSeek-generated figure set is not active for the current JCIM submission package.

Use the previous harmonized figure set instead:

`../frozen_jcim_style_v4`

Reason:

- The generated Figure 2 candidate was useful as an exploration, but the previous harmonized figure set is currently preferred for submission continuity and visual consistency.
- Figures in this folder should be treated as archived experiments unless explicitly reactivated.
